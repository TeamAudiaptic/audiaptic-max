# Source patches

This directory contains the Max patches and shared project resources that will form the Audiaptic Max integration.

Each event patch has its own directory so its interface and supporting materials can evolve independently.
